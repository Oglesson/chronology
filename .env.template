# Copy to .env.local and fill in.
# Local dev (Vite app talking to `npm run watch:mock` on :8080):
VITE_API_ENDPOINT=http://localhost:8080/chronology

# Auth0 application (Single Page Application) settings:
VITE_APP_CLIENTID=
VITE_APP_ISSUER_DOMAIN=
VITE_API_AUDIENCE_IDENTIFIER=
